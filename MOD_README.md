# Telegram iOS Mod

Модифицированная версия Telegram для iOS с дополнительными функциями.

## 🔧 Функции мода

### Resolver
- Показывает числовой ID пользователей, групп и каналов
- Включается в настройках мода

### Anti-Delete
- Сообщения, удалённые другими пользователями, помечаются как "deleted" вместо удаления
- Вы можете видеть оригинальный контент удалённых сообщений

## 📱 Доступ к настройкам мода

1. Откройте **Настройки** (ваш профиль)
2. Прокрутите вниз до секции **🔧 Mod Settings**
3. Включите нужные функции

## 🏗️ Сборка

### Требования
- macOS с Xcode 15+
- Bazel
- Python 3

### Локальная сборка (на Mac)
```bash
# Клонируйте репозиторий
git clone --recursive https://github.com/YOUR_USERNAME/Telegram-iOS-Mod.git
cd Telegram-iOS-Mod

# Сборка для симулятора (без подписи)
python3 build-system/Make/Make.py build \
  --configurationPath=build-configuration.json \
  --xcodeManagedCodesigning \
  --configuration=debug_sim_arm64 \
  --buildNumber=10001
```

### Сборка через GitHub Actions

1. Форкните этот репозиторий
2. Перейдите в **Actions** → **Build Telegram iOS Mod**
3. Нажмите **Run workflow**
4. После завершения скачайте артефакты

### Сборка с подписью (для установки на устройство)

Для сборки подписанного IPA нужно настроить секреты в GitHub:

1. Перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Добавьте следующие секреты:

| Секрет | Описание |
|--------|----------|
| `APPLE_CERTIFICATE_BASE64` | Base64 вашего .p12 сертификата |
| `APPLE_CERTIFICATE_PASSWORD` | Пароль от сертификата |
| `PROVISIONING_PROFILE_BASE64` | Base64 вашего .mobileprovision |
| `TELEGRAM_API_ID` | API ID от [my.telegram.org](https://my.telegram.org) |
| `TELEGRAM_API_HASH` | API Hash от my.telegram.org |
| `APPLE_TEAM_ID` | Team ID из Apple Developer |

#### Как получить Base64 сертификата:
```bash
base64 -i certificate.p12 | pbcopy
```

#### Как получить Base64 provisioning profile:
```bash
base64 -i profile.mobileprovision | pbcopy
```

## 📲 Установка IPA

### Без Apple Developer аккаунта
- **AltStore** - [altstore.io](https://altstore.io)
- **Sideloadly** - [sideloadly.io](https://sideloadly.io)

### С Apple Developer аккаунтом
- Через Xcode
- Через Apple Configurator

## ⚠️ Важно

- Для работы приложения нужны API ID и API Hash от [my.telegram.org](https://my.telegram.org)
- Без подписи IPA не установится на устройство
- Бесплатная подписка через AltStore требует переподписи каждые 7 дней

## 📁 Структура мода

```
submodules/TelegramUI/Sources/Mod/
├── ModSettings.swift           # Хранение настроек
├── ModSettingsController.swift # UI настроек
├── AntiDeleteManager.swift     # Логика Anti-Delete
├── ResolverHelper.swift        # Логика Resolver
└── ModIntegration.swift        # Инструкции интеграции
```

## 🔄 Обновление

При обновлении оригинального Telegram-iOS:
1. Merge изменений из upstream
2. Проверьте что файлы мода не конфликтуют
3. Пересоберите проект

## 📜 Лицензия

Этот мод основан на [Telegram-iOS](https://github.com/nicegram/nicegram-ios) и распространяется под той же лицензией.
